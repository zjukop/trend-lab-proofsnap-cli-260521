# ProofSnap CLI

Minimal local-first CLI starter for turning media URLs into transcript content packs.

## Quickstart

```bash
python -m venv .venv && source .venv/bin/activate
pip install -e .
proofsnap "https://example.com/video"
```

Run tests:

```bash
pytest -q
```
